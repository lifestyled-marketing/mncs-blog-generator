{
  "name": "mncs-blog-generator",
  "version": "1.0.0",
  "description": "Vercel serverless function that turns podcast transcripts into blog posts via a multi-step Claude pipeline.",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.30.1"
  }
}
